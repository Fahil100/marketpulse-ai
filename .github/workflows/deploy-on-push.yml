name: 🚀 Auto Deploy to Render on Push

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🔄 Trigger Render Deploy Hook
        run: |
          curl -X POST https://api.render.com/deploy/srv-d1cbut95pdvs73emhsf0?key=0H1DdNtEEgg
